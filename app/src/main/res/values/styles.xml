<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppFullscreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="spinnerNormal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:textColor">#564789</item>
        <item name="android:textSize">16dp</item>
    </style>

    <!-- 从下往上弹出式的选择控件动画 -->
    <style name="login_popup_window_anim_style">
        <item name="android:windowEnterAnimation">@anim/login_popup_show</item>
        <item name="android:windowExitAnimation">@anim/login_popup_hide</item>
    </style>
</resources>
