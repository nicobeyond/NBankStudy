<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nbank.study">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- <editor-fold desc="TBS"> -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="com.nbbank.permission.BROADCAST" />

    <!-- </editor-fold> -->
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".camera.CameraActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".tbs.TbsActivity" />
        <activity android:name=".cipher.SqlCipherActivity" />
        <activity android:name=".test.TestActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".camera2.Camera2Activity" />
        <activity android:name=".camera2.Camera3Activity" />
        <activity android:name=".camera2.JumpCamera3Activity" />
        <activity android:name=".logan.LoganActivity" />
        <activity android:name=".web.WebViewActivity" />
        <activity
            android:name=".PanActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" />
        <activity android:name=".image.ImageTestActivity"></activity>
    </application>

</manifest>